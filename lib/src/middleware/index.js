const sidedispatch = require('./sidedispatch');
const makeTargetMiddleware = require('./target');
const uuid = require('./uuid');

module.exports = {
    sidedispatch,
    makeTargetMiddleware,
    uuid,
};